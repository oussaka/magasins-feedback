'use strict';

/* jasmine specs for controllers go here */

describe('Controller: scoreFilEau', function() {
    var scope, controller, restService, $location;


    beforeEach(angular.module('scorefileau.controllers'));


    /*describe('scoreFilEau', function () {

        beforeEach(inject(function ($rootScope, $controller) {
            scope = $rootScope.$new();
            controller = $controller('scoreFilEau', {
                '$scope': scope
            });
        }));

        it('sets the name', function () {
            expect(scope.name).toBe('Superhero');
        });

        it('watches the name and updates the counter', function () {
            expect(scope.counter).toBe(0);
            scope.name = 'Batman';
            scope.$digest();
            expect(scope.counter).toBe(1);
        });

    });*/

  it('should ....', inject(function() {
    //spec body
    expect(4).toBe(1);
  }));

/*  it('should ....', inject(function() {
    //spec body
  }));*/

});
